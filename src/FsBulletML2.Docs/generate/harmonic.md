# 花（`Harmonic`）

放射 の 速さ を 角 で 変調 する 生成器。speed_i = r0 + A sin(k θ_i + φ) で、同じ コマ に 出た 弾 が k 枚 の 花弁 の 輪郭 を 描いて 広がる。軸 は `HarmonicAxes` に 生値 で 書き、`HarmonicSpec.create` が clamp して `HarmonicSpec` に する。

## 輪郭（`Figure`）

`Petal` / `Star` / `Heart` の 3 札。違う のは 速さ を 角 で 変調 する 式 だけ。

- `Petal` は 山 も 谷 も 丸い
- `Star` は 外 の 頂点 と 内 の 頂点 を 直線 で 結んだ 輪郭（`starAt`）。内/外 は `STAR_INNER`（0.382、正 五芒星 の 比）で、外/内 は その 逆数 1 / 0.382 = 2.618
- `Heart` は k を 輪郭 に 使わず、1 周 に 1 つ の くびれ。くびれ は 真上

`Figure.ofString` は 知らない 字 を `Petal` に 倒す。`figure` を 書かない 呼び手 は 今 の 花 の まま。`cardioid` は `heart` の 旧名 として 受ける —— 素 の r = 1 - cos θ は 卵 に しか 見えなかった ので 式 を 替えた が、字 で 名指し して いた 側 を 落とさない。

### 薔薇 を 落とした 理由

中心 から 見て どの 角 にも 半径 が 1 つ の 輪郭（`r = f(θ)`）しか 描けない ので、渦 を 巻いて 花びら が 重なる 薔薇模様 は 式 を どう 変えて も 出ない。`|cos(kθ/2)|` は 花 と 見分け が つかず、2 乗 は `(1 + cos kθ)/2` で 花 そのもの、全角 の 2k 枚 は 薔薇 で なく 菊 の 座 に なった。

### 星 を 逆数 余弦 で 描かない 理由

逆数 余弦（r = sqrt(1-α²)/(1 + α cos kθ)）は α を どこ に 振って も ★ に ならなかった。

- 0.85 では 細い トゲ 5 本 と 中心 の ダマ（外/内 12.3 の 閃光）
- 0.45 では 外/内 が ★ と 同じ 2.6 でも 山 が 丸い 5 弁 の 花

滑らかな 曲線 から 直線 の 辺 は 出ない。星 らしさ は 比 では なく 辺 が 直線 か で 決まる。

逆数 余弦 の 頃 の 記録：`Star` は 相乗 で なく 算術 の 平均 が r0 —— 1/(1 + α cos) の 平均 は 1/sqrt(1-α²) なので、sqrt(1-α²) を 掛ける と 平均 が r0 に 戻る。これ で 山 と 谷 の 比 が `Speed` から 外れる。素 の `r0 / (1 + α cos)` だと 山 が 6.67 r0 まで 伸びて、`Speed` が 高い 札 は 天井 で 全部 潰れた。`Speed` の 高い 札 で 山 が `MAX_SPEED` に 貼り付き、★ が 角 の 丸い 多角形 に 潰れて いた。折れ線 に 替えて 天井 に 当たらなく なり、いま は 山 が r0 の 1.82 倍 止まり。

### ハート を 素 の カージオイド で 描かない 理由

素 の カージオイド（r = 1 - cos θ）は 尖点 は 在る が 二つ山 に ならず、96 発 で 描く と 上 が へこんだ 卵 に 見えた（実測）。床 から 立ち上げて 外/内 を 9 倍 に して も 形 は 変わらなかった ので、深さ では なく 式 の 問題。尖点 が 在る だけ で 裾 が 広い。

くびれ の 深さ `betaOf` を 割る 1.5 は 星 と 同じ 都合 —— 面 が 返す 1.2 では 0.6 に しか ならない。

## 速さ（`speedAt`）

角 t での 速さ（`$rank = 0`）。振幅 0 は どの 札 でも 真円。

- 振幅（`amp`）は r0 - 0.5 で 止める。越える と 谷 の 弾 が 負 の 速さ で 逆走 し、0.3 では 敵 の 近く に 居座った
- `SPEED_LO`（0.3）は 床。星 の 内 の 頂点 は ここ に 当たる —— r0 = 1・α = 0.85 で 素 の 谷 は 0.285 で、床 で 止めて も 外/内 は 11.7 倍 残る
- `speedHi` は 天井。`$rank = 1` で `rankFactor`（0.3）倍 されて `MAX_SPEED` に 当たる 手前。`Speed` の 高い 札 で 星 の 山 が ここ に 当たり、尖り が 平ら に 潰れる。潰れた こと は 弾数 にも 形 の 門 にも 出ない ので、速さ の 最大 を 見る 門 で 止める

星 と ハート は 素 の 式 の 高さ が r0 と 揃って いない ので、1 周 平均（`starMean`・`HEART_MEAN`）で 割って 戻す。割り忘れ は 形 を 変えず に 速さ だけ を 動かす ので、尖り の 門 も 比 の 門 も 通って しまう（星 は 0.55 倍、ハート は 2 倍）。

`Petal` で 第 2 の 波 が 無い ときは 元 の 1 行 を そのまま 通る。`+ 0.0` で 済ませない のは、x + 0.0 は x = -0.0 の とき だけ 答え が 変わる 上 に、字 で 読めなく なる から。

## 第 2 の 波 と 層（`Folds2`・`Amplitude2`・`Layers`）

- 第 2 の 波 は `Folds` の 倍数 なら 対称 が 残って 畳めるが、互いに素 だと 畳めず 字 が 4 倍 に なる
- 深さ は 2 つ の 波 で 分け合う。足す と `amp` が 守って いる 不変（谷 >= 0.5・山 <= 天井）が 壊れる —— Speed 3 / Amplitude 2 / Amplitude2 1 で 山 5.70・谷 -0.70 の 両方 が クリップ した
- 取り分 0 は 第 2 の 波 が 無い のと 同じ で、`create` が `Folds2` も 0 に 倒す。倒さない と `symmetryOf` が gcd を 取って 畳み が 外れる —— Folds2 8・Amplitude2 0 は 見た目 が 5 回 対称 の まま gcd(5, 8) = 1 で 畳めず、字 が 4 倍 に なる
- 層 は 同じ 向き に 重ね、2 枚 目 以降 は 角 を 進めず 速さ だけ 変える。刻み 0 は 同じ 大きさ で 重ね、花弁 が 互い違い に 絡む。刻み 正 は 入れ子 で、内 と 外 が 分かれる
- 入れ子 は 外 の 層 に 合わせて 全体 を 縮める（外 を 伸ばす と 天井 に 当たる）。内 と 外 が 交わらない のは a < s / (2 + s)（外 の 谷 が 内 の 山 より 外）。Jev が 深い 振幅 を 返して も 読める ように、その 9 割 で 頭打ち に する
- 層 の 位相 の ずれ（`layerShift`）は `Folds` で なく `symmetryOf` で 割る。ハート は `Folds` を 輪郭 に 使わず 周期 が 2π なので、`Folds` で 割る と 半周期 の つもり が 1/10 しか ずれない

## 種（`Seed`）

`Folds2`・`Amplitude2`・`Layers`・`LayerPhase`・`LayerScale` の 5 本 を 表 から 一括 で 引く。種 は 名指し の 無い 欄 だけ を 埋める（`Arms = 0` が 既定 を 引く のと 同じ 決め方）。

- 表 は 比 と 層 の 6 組 x 深さ 3 の 18 通り
- 比 の 0 と 1 は 倍音 で 対称 が 残り、2 は 互いに素 で 畳めない。互いに素 は 単層 だけ —— 畳めない まま 層 を 重ねる と `<fire>` が 層 の 数 だけ 増える
- 互いに素 の 相手（`GOLDEN_PARTNER`）は 3:5 / 5:8 / 8:13 が フィボナッチ の 隣 で、比 が 黄金比 1.618 の まわり。7 は フィボナッチ の 外 なので 11 —— 11/7 = 1.571 で、12/7 = 1.714 より 近い
- ハート は 1 回 対称 で 畳めず、層 を 重ねる と `<fire>` が 層 の 数 だけ 増える（3 枚 で 480 本・82,842 バイト）。種 では 重ねない。名指し すれば 重なる

## 並び（`Arms`・`Blooms`）

`Arms` は 1 波 の 弾 の 数。8..160、0 は 花弁 x 24 を `Blooms` で 割った 数（上限 160、下限 16）。24 発 では 広がる と 点 が 離れ、同じ 向き の 点 が 線 に 並んで 花 で なく 放射 の 線 に 見えた。

`Blooms` は 何 か所 で 咲かせる か（1..8）。ここ が 決める のは 弾数 の 割り当て だけ で、撒く のは `FsBulletML2.Scatter.apply` —— あちら は Core しか 見ない ので 花 に 限らず 使える。2 以上 に した まま 撒かない と、腕 が 割られた だけ の まばらな 1 輪 が 出る。

- 花 を 2 つ 以上 咲かせて も 同時 に 居られる 数 は 変わらない。名指し が 無い とき は 1 輪 ぶん を 数 で 割る —— 割らない と `fit` が `wait` を 伸ばし、輪 と 輪 の 間 が 3 秒 空いて「まばらな 花」に なる
- 名指し して も 咲かせる 数 で 頭打ち。1 波 は まるごと 同時 に 居る ので、ここ を 割らない と 160 x 8 が 1 コマ に 出て、`fit` は `wait` しか 伸ばせず 直せない
- 層 では 割らない。割る と 1 層 あたり 60 点・40 点 に なり、半周期 ずらした 層 が 互い に 絡んで 点 の 散らばり に しか 見えなかった（同じ 式 で 120 点 の まま なら 花弁 が 互い違い に 編まれた ロゼット に なる）。増えた 弾 は `fit` が 間隔 で 払う
- 層 を 重ねる とき だけ 対称 の 回数 の 倍数 に 丸める。k = 7 は 160 が 7 で 割れず 畳めない まま 層 の 数 だけ 増えた（3 枚 で 480 本）。154 に 丸める と 畳める。単層 は 丸めない —— 丸める と 書かない 呼び手 の 字 が 動く

## 畳み（`foldBy`・`firedShots`・`ring`）

k 回 対称 の 札 は 1/k 周 だけ 書いて `repeat` で 回す。`sequence` の 刻み は `repeat` を 跨いで 足し込まれる ので、n 回 で ちょうど 1 周 する。腕 m の 速さ は 対称 から 腕 0 と 同じ。

- 畳める のは 輪郭 が n 回 対称 で、`Arms` が n で 割り切れる とき だけ。ハート は 1 回 対称 で 畳めず、k = 7 は `Arms` が 160 で 割り切れない（160 / 7）。4 つ の k の うち 3 つ（3 / 5 / 8）が 畳まる
- 対称 の 回数（`symmetry`）は 第 2 の 波 が 入る と 2 つ の 回数 の 最大公約数 に 落ちる。`create` が `Arms` を 丸める とき と `Harmonic` が 畳む とき の 両方 が ここ を 読む
- 畳む と 頭 と 最後 の 1 発 が 同じ 向き に なる ので 1 発 多い。頭 は 1 波 に 1 度 しか 置けず（`absolute 0` か、回す とき の 足し込み）、残り を n 回 の `repeat` で 割る と 1/n 周 x n = 1 周 ちょうど で 頭 に 戻る。120 発 の うち 1 発 が 重なる だけ なので、字 が 5 倍 小さく なる 代金 として 払う
- 1 波 の 弾数 `shotsPerWave` は 畳んだ とき `(Arms + 1) x Layers`

`<fire>` の 数 と 字 の 長さ（Blooms 1）：

| k | `<fire>` | 字 の 長さ |
|---|---|---|
| 3 | 72 -> 25 個 | 12,665 -> 5,167 |
| 5 | 120 -> 25 個 | 20,921 -> 5,167 |
| 7 | 160 -> そのまま（160 / 7 が 割り切れない） | |
| 8 | 160 -> 21 個 | 27,801 -> 4,399 |
| ハート | どの k でも そのまま（1 回 対称） | |

## 回転（`turnDeg`・`TURN_TERM`）

1 輪 の 弾 が 同じ 角 で 曲がる と、位置 は 全員 に 共通 の 因子 が 掛かる だけ なので、花 は 形 を 保った まま ω/2 で 回り ながら 広がる（曲がる 120 コマ で Spin 1 は 45 度、2 は 60 度）。曲げる ほど 広がり が 縮む（真っすぐ の 2 sin(ωt/2) / ωt 倍。Spin 1 の 120 コマ で 90%）ので 1 度 で 止める。

撃つ 向き だけ を 輪 ごと に 回して いた とき、花 1 輪 は 向き を 変えず に 広がり、隣 の 輪 と 少し ずれて 見える だけ で 回転 に 見えなかった。

回す とき は 頭 も `sequence` に して、`360 / Arms` に 輪 の 間 に 花 が 回る 角（ω/2 x wait）を 足す。前 の 波 の 最後 の 弾 は 頭 から 1 刻み 手前 なので、刻み を 1 つ 足す と 頭 に 戻る。先 に 出た 花 と 向き が 揃い、何重 の 花 が 1 つ として 回る。`defAction` に θ0 を `$1` で 渡す 形 は 波 を 跨いで 足し込めない（`repeat` は 毎回 同じ 引数 を 渡す）。

曲がる のは `TURN_TERM`（120 コマ）まで。以後 は 真っすぐ 飛ぶ ので 花 は 回り終えた 向き の まま 広がり、面 の 外 へ 出る。曲がり 続ける と 遅い 弾 は 半径 v / ω の 円 を 描いて 出て 行かない。台本 は 終わらせない —— 面 は 終えた 弾 を 頭 から 走らせ直す ので、終わる と また 曲がり出す。

## 難度（`$rank`）

- 試験 の `Felt` は `$rank = 1` で 走る ので、返る 速さ は 基準値 の 1.3 倍 —— 床 は 0.39、天井 は `MAX_SPEED`。基準値 の まま 書く と 床 は 1 発 も 当たらず、天井 は 健全 な 弾 まで 拾う
- 輪 と 輪 の 間（`waitOf`）は 40 / 32 / 24 / 16 コマ。詰める と 1 輪 ずつ の 輪郭 が 重なって 読めない
- 180 コマ で 消して いた とき、いちばん 速い 弾 でも 敵 から 489 px で 消え、520 px 先 の 自機 に 1 発 も 届かなかった

## 上界（`aliveBound`・`fitTo`）

同時 に 居る 弾 の 上界 は `$rank = 1.0` で 見る。弾 ごと に 速さ が 違う ので、寿命 も 弾 ごと に 取って 足す。`Vanishing` が 無ければ 画面 を 抜ける まで 生きる。種 は 1 波 に `Blooms` 発 しか 出ず、咲いたら 消える ので 数 に 入れない。

`fitTo` は 越える なら `wait` を 伸ばす。花弁 の 数 と `Arms` は 形 そのもの なので 減らさない。上限 は 引数 —— 混ぜ の 相手 が 居る とき は 半分 に なる。

## 種 の 閾値（`HarmonicSpace`）

種 18 通り が「ダメ」に 落ちて いない こと を 見る。美しい は 測れない ので、弾く のは ダメ の 側 だけ。閾値 は 速さ と 深さ を 4 通り 振った 18 x 4 = 72 通り の 実測 から、良い 側 が 全部 通る いちばん 厳しい 値 から ゆるい ほう へ 2 割。

| 見る もの | 実測 | 閾値 |
|---|---|---|
| 外周 の 山 | 5 以上 | 2 以上（1 は 真円） |
| 外/内 | 2.02 .. 4.91 | 1.3 .. 6.0 |
| 標本 | 8 .. 15 | Arms / Folds2 >= 4 |
| 床 / 天井 | 0 | 0（潰れた 弾 が 1 発 も 無い） |
| 規則 | 0.680 以上 | 0.55 以上 |
| 字 | 最大 27,801 バイト | 40,000 未満（札 x 枚数 x 種 216 通り） |
| 上界 | 最大 898 | MAX_ALIVE 以下（fit 後） |

- 外/内 の 上限 は 4 に しない。第 2 の 波 の 無い 深い 花（Speed 1 / Amplitude 2）が 元 から 4.9 あり、4 で 切る と いま の 花 を 弾く
- 余白（一面 に 埋まる）は 測って いない —— 72 通り 全部 が 空き 6 割 超 で、落ちる 例 が 無く 較正 できなかった
- 床 / 天井 を「3 割 未満」に して いた ときは、足す 式 に 戻して クリップ が 出て も 3 割 に 届かず 素通り した
- 潰れ を 先 に 見る。潰れる と 外/内 も 規則 も 崩れる ので、形 を 先 に 見る と そちら が 先 に 落ちて 床 と 天井 の 行 に 届かない（足す 式 で 踏んだ）
- 規則 は 外周（向き ごと に いちばん 速い 弾）に 当てる。層 を 重ねた 輪郭 に そのまま 正弦 を 当てる と、半周期 ずらした 層 が 第 1 の 波 を ちょうど 裏返して 打ち消し合い 0 に なる（層 1 枚 ずつ は 規則的 なのに）

## 絵（`HarmonicGrid`）

1 波 を 絵 に して 1 枚 に 並べる。形 の 判断 を 8787 の 目視 だけ で やって いて、ハート で 2 周・星 で 2 周・薔薇 で 4 周 外した。絵 が 試験 の 中 で 出れば その 全部 が 1 回 で 済む。

## 指紋（`GoldenHarmonic`）

花 が 出す 字 を まるごと 固定 する。`GoldenShape` は `Generate.generate`（5 つ の 型）を 見て いて、花 は 1 通り も 通らない。収録 の ゴールデン（DanmakuLab 側）も family / kind / folds / ways しか 見て いない。

赤 に なった ときは 2 通り —— 直し が 答え を 変えた か、変える つもり の 直し か。後者 なら 指紋 を 置き直して、何 を 変えた か を commit に 書く。

## 較正

### 試験 の 閾値 と 実測

| 試験 | 見る 値 | 実測 | 閾値 / 備考 |
|---|---|---|---|
| `星 の 辺 は 直線` | 続く 3 点 の 曲がり の 中央値 / 山 の 半径（96 発。振幅 1.0 以上 の 9 通り） | 星 0.00045 .. 0.00219、ハート 0.00159 .. 0.00229、花 0.00770 .. 0.00982 | 床 0.005 は 星 と 花 の あいだ。ハート とは 分けない —— くびれ が 1 つ で 辺 が 長い ので 同じく 曲がり が 小さい。あちら と 分ける のは 尖り の 門。振幅 0 の 真円 も 0.00214 |
| `星 だけ 逆数 が 正弦 に乗る` | `recipScore - foldScore`（4 札 x 振幅 3 段 x 速さ 3 段 = 36 通り） | 星 の 差 の 最小 +0.083（振幅 1.00）、星 以外 の 最大 +0.021（ハート。k を 輪郭 に 使わない ので fold も recip も ~0）、花 の 差 -0.027 .. -0.146 | 床 0.05 は その 2 つ の あいだ。`recip` 単独 では 振幅 の 浅い 花 も 0.85 まで 出る |
| `ハート の 下 は 尖る` | 山 の 8 割 より 速い 弾 の 割合（振幅 3 段 x 速さ 3 段） | ハート 0.031 .. 0.115、星 0.094 .. 0.219、花 0.323 .. 0.448 | 床 0.15 は ハート と 花 の あいだ。星 と は 分けない —— 分ける のは 逆数 の 門 |
| `速さ の 平均 は 花 と 揃う` | 平均 の 速さ（振幅 1.51） | 速さ 0：花 1.300・星 1.300・ハート 1.302 / 速さ 1：花 1.950・星 1.950・ハート 1.953 / 速さ 3：花 3.250・星 3.251・ハート 3.244 | |
| `ハート の くびれ は 1 点` | 96 発 の うち 谷 に 並ぶ 数 | 花 は k 個、星 は 最大 17 個 | |
| `入れ子 の 層 は 交わらない` | 速さ を 小さい 順 に 並べて 隣 と 比べる 形（使わなかった） | 頭打ち を 外して 交わらせた ほう が 1.806、正しい 実装 が 1.253 | 入れ替わり が 見えず、壊した ほう が 大きく 出た |

### 輪郭 の 4 札 に 当てた 変異（`HarmonicTests`）

| 変異 | 赤 に なった 試験 | 備考 |
|---|---|---|
| 星 の 正規化 sqrt(1-a^2) を 1 に | 2 本 | |
| 星 の 分岐 を 花 に 倒す | 3 本 | |
| 星 の 抉り alpha を 0 に | 3 本 | |
| 知らない 字 の 既定 を Star に | 1 本 | |
| 速さ の 床 を 0 に | 1 本 | 1 周 目 は 見る 門 が 無く 緑 |
| 速さ の 天井 を MAX_SPEED そのもの に | 1 本 | |
| recip の 逆数 を そのまま に | 3 本 | |
| recip の ほぼ 0 落とし を 外す | 1 本 | 1 周 目 は 緑。材料 が ちょうど 0 で、逆数 の 無限 を `IsFinite` が 先 に 落として いた |
| ハート を 素 の カージオイド に 戻す | 2 本 | |
| ハート を HEART_MEAN で 割らない | 1 本 | 1 周 目 は 緑。形 を 変えず に 速さ だけ が 2 倍 に なる |
| ハート の くびれ beta を 0 に | 5 本 | |
| ハート の くびれ を 真下 に | 1 本 | |
| ハート を 床 から 立ち上げない | 1 本 | 1 周 目 は 緑。形 を 変えず に 通り、谷 が 床 で 切られて 平ら に なる |

### 畳み に 当てた 変異（`k 回 対称 の 札 は 1/k 周 だけ 書く`）

| 変異 | 赤 に なった 試験 | 備考 |
|---|---|---|
| 畳まない（いつも None） | 2 本 | |
| ハート も 畳む | 2 本 | |
| 割り切れなくて も 畳む | 4 本 | |
| `repeat` の 回数 を k+1 に | 5 本 | |
| 体 を 1/k 周 より 1 本 短く | 7 本 | |
| 上界 が 畳み を 数えない | 2 本 | |

### 咲かせる 数 に 当てた 変異（`n か所 で咲き、1 は 敵 の位置 のまま`）

| 変異 | 赤 に なった 試験 | 備考 |
|---|---|---|
| `create` で 腕 を 咲かせる 数 で 割らない | 同時 に MAX_ALIVE 以下 | |
| `aliveBound` の `* Blooms` を 落とす | 同時 に MAX_ALIVE 以下 | |
| `bloomCenters` で いつも 束ねる | n か所 で 咲く | |
| `bloomCenters` で 1 つ も 束ねない | n か所 で 咲く | |
| `bloomCenters` で 全部 の 弾 を 見る | 何 も 生まれて いない コマ は 空 | |
| `aliveBound` の 下限（`Arms * Blooms`）から `Blooms` を 落とす | なし | 腕 の 上限 を 咲かせる 数 で 割った ので、1 波 は 160 発 を 越えられず この 下限 は 効かない |

### 第 2 の 波 と 層 と 種 に 当てた 変異（`HarmonicTests`）

| 変異 | 赤 に なった 試験 | 備考 |
|---|---|---|
| create の layers の max 1 を 外す | 指紋 ほか 10 本 | Layers 0 で 1 波 が 0 発 に 数えられる |
| layerShift を 常 に 0 | 層 は 同じ 角 に… / ハート の 層 は 半周 ずれる | |
| Arms を 層 で 割る に 戻す | 解像度 を 割らず に… / 7 枚 も 畳める / 標本 | |
| Arms を 対称 の 倍数 に 丸めない | 7 枚 も 畳める | |
| 種 の ハート 除外 を 外す | 種 は ハート に 層 を 足さない | |
| symmetry の gcd を Folds に | 互いに素 の 第 2 の波 は 畳めない | |
| a1 を a に して 足す 式 に 戻す | 深さ は 足さず に 分け合う | |
| create の 取り分 0 の 倒し を 外す | 取り分 0 は いま と同じ | |
| layerShift を Folds で 割る | ハート の 層 は 半周 ずれる だけ | |
| 入れ子 の 振幅 の 頭打ち を 外す | 入れ子 の 層 は 交わらない / 天井 と 床 | |
| 入れ子 を 外 に 合わせて 縮めない | 入れ子 でも 天井 と 床 に 当たらない | |

### 種 の 閾値 に 当てた 変異（`HarmonicSpace`）

| 変異 | 赤 に なった 行 | 備考 |
|---|---|---|
| Arms を 層 で 割る に 戻す | 標本 | |
| a1 を a に して 足す 式 に 戻す | 床・外/内 の 上限・規則 | |
| r0 の 傾き を 0.5 から 1.0 に | 天井 | |
| 振幅 を 0 に（真円） | 山 | 外/内 の 下限 は その 後ろ で 届かない |
| 種 の 重ね の 位相 0.5 を 0.37 に | 規則 | |
| Arms を 対称 の 倍数 に 丸めない | 字 | |

### 指紋 に 当てた 変異（`GoldenHarmonic`）

| 変異 | 赤 に なった 試験 | 備考 |
|---|---|---|
| STAR_INNER 0.382 -> 0.383 | 指紋 だけ | |
| firedArms の m -> m - 1 | 2 本 とも | 上界 が 変わって wait も 動く |
